# Lib

Shared utility modules. All functions here are pure (no React dependencies
except `auth.tsx`) and independently testable.

## Modules

| File | Purpose | Tests |
|------|---------|-------|
| `auth.tsx` | AuthProvider context + useAuth hook. USDX-392: auth via httpOnly session cookie (no token in localStorage); localStorage caches only the non-sensitive v5 Staff profile for synchronous restore, re-validated by GET /auth/me; logout POSTs /auth/logout for server-side revoke | `__tests__/auth.test.tsx` |
| `types.ts` | Domain types: Staff, Customer, OtcMintTransaction, OtcRedeemTransaction, OtcStatus, Network, CustomerType/Role, StaffRole, ReportRow, DashboardSnapshot, PaginatedResponse, ApiError | — |
| `validators.ts` | Pure form validators: validateLoginForm, validateCustomerForm, validateStaffForm, validateOtcMintForm, validateOtcRedeemForm, **validateMintModeReason / validateMintModeDurationHours / validateMintAllowedEmail / validateMintTestModeForm** (USDX-639 — alasan wajib **minimal 10 karakter** (`sot/api/mint-mode.yaml § SetMintMode`, CHECK yang sama ada di DB; konstantanya milik mode mint sendiri, TIDAK diturunkan dari `LEDGER_REASON_MIN_LEN` karena dua kontrak berbeda yang kebetulan sepakat hari ini), durasi bilangan bulat 1..24 jam — pecahan ditolak di klien karena layar ini tidak punya satuan yang lebih halus dan menerimanya hanya memindahkan penolakan ke server, dan `allowedEmailDraft`: alamat yang MASIH di kotak dan belum ditambahkan ikut divalidasi supaya email setengah diketik tidak terkirim diam-diam, sementara kotak KOSONG bukan kesalahan karena daftar boleh kosong), **validateMinMintIdr + feeConfigErrorField** (USDX-637 — lantai keras Rp 10.000 DISALIN dari kontrak backend, tanpa plafon atas karena kontrak tidak menetapkan satu pun; `feeConfigErrorField` membaca nama field dari message 422 dan mengembalikan `null` saat message menyebut nol atau lebih dari satu field, karena menebak akan mengirim operator memperbaiki angka yang tidak dikeluhkan server), validatePhone, validateWalletAddress, validateLedgerEntryForm (+ per-field validateLedgerAmount/Reason/OccurredAt/Currency/EntryType, LEDGER_ERROR_FIELD mapping server codes → fields), validateAttestationUploadForm, **validateScreeningReason** (alasan keputusan screening — 10..1000 karakter, mengembalikan nilai ter-trim supaya `"          "` tidak lolos: panjangnya persis cukup melewati `@MinLength(10)` di server tapi kosong bagi pemeriksa yang membacanya bertahun kemudian), **validateKycRejectReason** (USDX-610 — alasan tolak KYC, angkanya DITURUNKAN dari `KYB_REJECT_REASON_MIN`/`MAX` alih-alih ditulis ulang: dua angka yang harus sama tapi ditulis dua kali akan berbeda suatu hari, dan yang menanggungnya nasabah yang menerima email penolakan tanpa keterangan) | `__tests__/validators.test.ts`, `__tests__/screening.test.ts` **USDX-605**: `validateKybForm` now covers the whole Pasal 33 (3) UBO block and the four Pasal 25 (1) b entity fields. Every limit is the CONTRACT's, never a stricter one — alias 200, birth place 100, employer address 500, employer phone 32 with **no pattern at all**, because `CreateKybUboDto` has none and a client rule stricter than the contract refuses input the server accepts. |
| `format.ts` | Number/date/hash formatting: formatWibClock (USDX-639 — `HH:MM WIB` untuk banner mode uji: jendelanya tidak pernah lebih dari 24 jam, jadi tanggal hanya menambah kata tanpa menambah jawaban), formatAmount, formatUsdxAmount, formatIdrAmount, formatDate, formatShortDate, formatRelativeTime, formatRate, formatSpreadPct, shortHash. **BNI (USDX-631)**: formatBniPostDate re-punctuates bank stamps (`yyyyMMddHHmmss` / `yyyyMMddHHmm` / `yyyyMMdd`, WIB) WITHOUT going through `Date` — null/MALFORMED → "—", never Invalid Date; formatBankAmount picks Rp / $ from the account currency; formatWibDateTime renders a UTC ISO instant in WIB with a `WIB` suffix | `__tests__/format.test.ts` |
| `status.ts` | OTC status → UI mapping: getOtcStatusConfig, isOtcTerminal | — |
| `csv.ts` | CSV generation: exportToCsv, buildCsvContent — escapeCsvCell prefixes formula chars (`=`, `+`, `-`, `@`) with a single quote to prevent injection; a bare `\r` triggers quoting like `\n`; `exportToCsv(..., { withBom: true })` prepends a UTF-8 BOM (opt-in — existing exports are byte-identical) | `__tests__/csv.test.ts` |
| `explorerUrl.ts` | Block explorer deep-links: buildTxExplorerUrl(blockExplorerUrl, hash), buildAddressExplorerUrl — base URL comes from GET /api/v1/chains, never hardcoded | `__tests__/explorerUrl.test.ts` |
| `safeUrl.ts` | Safe Wallet UI deep-links: buildSafeUrl({chainId, safeAddress, safeTxHash}), safeTxUrl({chain, safeType, safeTxHash}) (resolves the address by safeType, returns null when unbuildable), CHAIN_PREFIX_BY_ID | `__tests__/safeUrl.test.ts` |
| `chainLinks.ts` | findChainConfig(configs, chain) + resolveOnChainLinks(row, chains) → `{explorerHref, safeHref}` for Mint/Burn rows (composes explorerUrl + safeUrl + the chains config) | `__tests__/chainLinks.test.ts` |
| `dateRange.ts` | Pure date-range rules shared by `components/DateRangeFields` and its callers: validateDateRange (FORMAT → ORDER → MAX_DATE → MAX_DAYS), inclusiveDaySpan (UTC calendar arithmetic, DST-proof) | `components/__tests__/DateRangeFields.test.tsx` |
| `transparency.ts` | Reserve-ledger helpers. **Exact decimal money** on BigInt cents (parseAmountToCents, centsToAmount, addAmounts, formatAmountDecimal, formatLedgerAmount, isNegativeAmount) — numeric(30,2) does not fit a JS float. **WIB day** (wibToday, isFutureWibDate) mirroring the backend's wib-day util so `occurredAt` validation agrees with the server between 00:00–07:00 WIB. **newIdempotencyKey** — CSPRNG-backed UUID minted once per ledger form-filling attempt (never per click); throws rather than falling back to `Math.random()`, since a colliding key would swallow a genuine second entry. **looksLikePdf** — reads the file's first bytes, because an upload's name and MIME type are both picker-controlled (`{name:'evil.pdf', type:''}` passes every check that reads them); returns `null` for "could not read", which callers must not treat as a pass. Attestations: isActiveAttestation / activeAttestations (filters `revokedAt`), getPeriodParts, formatPeriod, formatOccurredAt | `__tests__/transparency.test.ts` |
| `kybDocumentUpload.ts` | KYB document upload rules (USDX-546) — TRANSCRIBED from the deployed backend, not chosen here: `KYB_DOCUMENT_SLOT_DOC_KINDS` (response slot `akte` → request `docKind` `kyb_akte`, the two vocabularies' only meeting point), the **5 MiB** ceiling (`MAX_FILE_SIZE_BYTES`) and the **PDF / JPEG / PNG** whitelist (`KYB_ALLOWED_CONTENT_TYPES` — no `image/heic`, which is valid for a KYC photo and not for a KYB document), `validateKybDocumentFile` (name / MIME / size, with an extension fallback for the empty-`type` drag-and-drop case), `sniffKybDocumentType` + `checkKybDocumentBytes` (magic bytes at OFFSET 0, mirroring the backend's `file-signature.ts`, because the name and the MIME are both picker-controlled and only the first bytes are not; unreadable content is NOT a pass), and `describeKybUploadFailure` (every server code → a sentence naming its CAUSE, so an operator holding a customer's document knows whether the problem is the file or us) | `__tests__/kybDocumentUpload.test.ts` **USDX-605**: the map gained the three Pasal 27 (1) b slots, and gained a SECOND map for UBO documents (`KYB_UBO_DOCUMENT_SLOT_DOC_KINDS`, prefix `kyb_ubo_`) — separate rather than merged, because the two hang off different tables and different endpoints, and one combined enum would let `kyb_akte` be posted to a route with no column to store it. `validateKybUboDocumentFile` + `declaredPhotoContentType` split the type whitelist per slot: the two photo slots accept HEIC (an iPhone hands you HEIC) and the two document slots refuse it, mirroring the backend's two verification primitives. `describeKybUploadFailure` takes the accepted-type label so the message names the whitelist that actually applied. |
| `screening.ts` | Screening DTTOT & DPPSPM (USDX-588). Peta label enum ditulis `Record<Enum, string>` sehingga nilai union baru tanpa labelnya MENGGAGALKAN build alih-alih menghasilkan sel kosong. `formatScore` mengembalikan `null` untuk skor kosong, bukan `"0%"` — skor kosong hanya terjadi pada `LIST_UNAVAILABLE`, dan nol persen terbaca sebagai "sudah dibandingkan dan sangat berbeda", kebalikan artinya. `scoreBarFraction` meregangkan bilah dari ambang **0.85** ke 1, bukan dari nol, karena seluruh antrean berada di atas ambang. **`previewSanctionCsv` adalah SALINAN `backend/src/modules/screening/sanction-csv.ts`**, sampai ke nomor barisnya (header = baris 1, seperti Excel menghitung): ia BUKAN pengganti pembaca di server — server tetap membaca ulang dan tetap yang menentukan — melainkan ada supaya berkas cacat ketahuan SEBELUM satu versi DRAFT terlanjur dibuat, sebab impor dipecah tiga langkah dan tidak ada endpoint untuk menghapus versi DRAFT. `chunkSanctionCsv` memotong hanya di batas BARIS lewat pemisah yang sadar tanda kutip (baris baru di dalam nilai terkutip bukan akhir baris — kolom `address` daftar sanksi memuat alamat berbaris ganda) dan mengulang baris header pada SETIAP potongan, karena server tidak mengingat keadaan antar panggilan dan potongan tanpa header akan menganggap entri pertamanya sebagai nama kolom. `SCREENING_ERROR_MESSAGES` memetakan tiap `code` bisnis ke kalimat yang menyebut SEBABNYA. **`summariseSubjectScreening` (USDX-610)** meringkas hasil SATU subjek untuk halaman review KYC/KYB, dan menghitung "daftar mana yang belum tercek" TERBALIK: baris `LIST_UNAVAILABLE` tidak membawa jenis daftarnya (CHECK `screening_results_row_shape` mewajibkan `list_id` NULL, dan `listType` pada response datang dari join), jadi yang bisa dijawab dengan pasti adalah jenis daftar wajib mana yang tidak punya SATU PUN hasil sungguhan. Daftar yang pernah tercek lalu gagal dibaca belakangan tetap dihitung tercek — buktinya sah — sementara kegagalannya tetap dilaporkan lewat `unavailableCount` | `__tests__/screening.test.ts` |
| `redeemApprovals.ts` | Aturan murni gerbang Persetujuan Pencairan (USDX-669). **Uang IDR eksak**: `parseIdrToCents` / `centsToIdr` / `formatIdrExact` (ejaan id-ID `Rp 1.500.000,50`, dikelompokkan atas STRING) / `formatUsdxExact` (buang nol ujung — `100.000000 USDX` membuat operator menghitung digit untuk memastikan itu seratus, bukan sejuta) — `numeric(20,2)` tidak masuk ke JS number dan `0.1 + 0.2` bukan `0.3`. Tekniknya persis `transparency.ts` dan SENGAJA tidak diimpor dari sana: itu kontrak lain (`numeric(30,2)`, boleh negatif, mata uang bebas) yang hari ini kebetulan sepakat soal dua desimal, dan menumpanginya berarti perubahan pada kontrak transparansi diam-diam memindahkan aturan uang gerbang payout — preseden `canDecideScreening` vs `canReviewKyc`. Nominal tak terbaca dikembalikan APA ADANYA, tidak pernah dipaksa jadi `0`, karena `0` di layar ini BERBUNYI "semua wajib disetujui". **Arah ambang**: `holdsEveryPayout`, `classifyThresholdChange` (`raised-from-zero` dipisah dari `raised` karena ia satu-satunya perubahan yang mengubah KATEGORI — dari "setiap rupiah dilihat manusia" jadi "sebagian tidak dilihat siapa pun"), `requiresRaiseConfirmation` (SETIAP kenaikan, superset AC; ditulis sebagai DAFTAR-TOLAK — hanya `lowered` dan `unchanged` lewat — supaya `invalid` MENAHAN alih-alih melepas: versi daftar-izin mematikan satu-satunya pagar sebelum melonggarkan payout persis saat nilai sekarang tak terbaca, mis. `"??"` / `"1,000,000"` / `"1000000.000"` / string kosong saat `GET controls` gagal). **Validasi**: `validateRejectReason` / `validateThresholdReason` (3..500, batas kontrak, mengembalikan nilai ter-trim supaya `"   "` tidak lolos ke `payout_issue_reason`), `validateApproveNote` (opsional; kotak kosong jadi `undefined`, bukan string kosong yang di `activity_log` terbaca seperti catatan), `validateThresholdAmount` (`^\d+(\.\d{1,2})?$`, negatif ditolak). **`queueBurnTxHref`** membangun tautan explorer untuk `burnTxHash` baris ANTREAN, yang tidak membawa `chain` sendiri (hanya detailnya): tepat SATU rantai terkonfigurasi ⇒ tautan dibangun, lebih dari satu ⇒ `null` dan TIDAK menebak, karena tautan ke explorer rantai yang salah menjawab "transaksi tidak ditemukan" untuk burn yang sebenarnya ada dan pencairan lalu ditahan atas bukti yang tidak pernah dicari di tempat yang benar. **`redeemApprovalErrorMessage`** memetakan per KODE dulu (`ALREADY_APPROVED` / `INVALID_ORDER_STATE`, keduanya 409 tapi tindak lanjutnya beda) lalu per STATUS — urutan itu penting karena backend USDX-668 dikerjakan paralel dan ejaan kode untuk 403 belum bisa dipastikan, jadi kasus peran bersandar pada STATUS supaya AC "403 tampil sebagai pesan manusia" tetap terpenuhi apa pun kode yang akhirnya dipilih server; pesan 400 dilewatkan apa adanya karena ia menyebut field yang harus dibetulkan | `__tests__/redeemApprovals.test.ts` |
| `mintMode.ts` | Mode mint PROD/UJI (USDX-639, USDX-654). `errorDetailList` membaca rincian dari `error.details` sebuah 422 — HANYA kalau `details` memang array string. `MINT_MODE_TEST_ENV_INCOMPLETE` memakai SATU kode untuk lima gerbang bundle uji, jadi isi `details` berbeda-beda (nama field, nama env, atau kalimat masalah dari `TestBundleVerifier`) dan semuanya ditampilkan apa adanya; bentuk lain diabaikan, bukan diterjemahkan, karena rincian yang salah baca membuat orang memperbaiki hal yang bukan penyebabnya. `isEip55Address` + `validateTestBundleAddress` + `conflictingTestBundleAddresses` menyalin gerbang alamat backend (`isChecksumAddress`, `getInvalidTestBundleAddresses`, pemeriksaan tabrakan): huruf kecil semua DITOLAK dan klien tidak menormalkannya, karena checksum adalah satu-satunya pemeriksaan yang menangkap salah ketik satu karakter sebelum alamatnya dipakai mencetak token — sengaja lebih ketat daripada `validateUserAddressField` di `validators.ts`, yang longgar untuk alamat dompet nasabah. `conflictingTestBundleAddresses` menolak token uji yang beralamat sama dengan Safe uji, tapi **membiarkan Safe staff = Safe manager** (USDX-655): bundle dev memakai satu alamat untuk keduanya, dan satu Safe fisik kini satu antrean karena backend mengunci antrean propose pada alamat. Tabrakan dengan alamat PRODUKSI tetap milik server: klien tidak menebak alamat produksi | `__tests__/mintMode.test.ts` |
| `utils.ts` | cn() — Tailwind class name merge utility (clsx + tailwind-merge) | `__tests__/utils.test.ts` |

## Rules

- All functions must be pure where possible (no side effects)
- All business logic must have unit tests with positive/negative/edge case coverage
- Types in `types.ts` define the API contract — keep in sync with MSW handlers
- Validators return `{ valid: boolean, errors: Record<string, string> }` — components display `errors` per field
- `auth.tsx` resolves Staff via in-memory `findStaffByEmail` from `src/mocks/handlers.ts` — no HTTP call inside AuthProvider, avoids ordering against MSW init
