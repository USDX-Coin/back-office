# Features

Feature-based modules. Each feature is a self-contained unit with its own
pages, modals, hooks, and types.

## Structure per Feature

```
feature-name/
  ├── FeaturePage.tsx          # Main page component (default export)
  ├── FeatureModal.tsx         # Add/Edit modal (where applicable)
  ├── FeatureDeleteDialog.tsx  # Destructive confirmation (where applicable)
  ├── FeatureFilterToolbar.tsx # Custom toolbar (passed to DataTable.filterToolbar)
  ├── hooks.ts                 # TanStack Query hooks for data fetching
  └── __tests__/               # Vitest integration tests
```

## Conventions

- **Pages** are route-level components, default-exported
- **Hooks** use TanStack Query (`useQuery`, `useMutation`) with query keys
  namespaced by feature (e.g., `['customers']`, `['otc', 'mint', 'recent']`)
- **Modals** use shadcn `Dialog`; Esc + outside-click are disabled while a
  mutation is in flight; reset on open via `useEffect` + `key` reset pattern
- Business logic (validation, formatting, status mapping) lives in
  `src/lib/`, NOT in feature files
- Each feature fetches its own data — no prop-drilling from parent layouts

## Features

| Feature | Pages | Auth Required | Description |
|---------|-------|---------------|-------------|
| `auth/` | LoginPage | No (PublicRoute) | Split-screen login (gradient hero panel + form). |
| `dashboard/` | DashboardPage | Yes | KPIs + recent activity. |
| `users/` | UsersPage + UserDetailPage + ActivationStatusSection | Yes | Customer directory (KYC + activation filters) + per-user detail with admin resend-activation. |
| `staff/` | StaffPage + StaffModal + StaffDeleteDialog | Yes | Internal team CRUD (admin-gated server-side). |
| `kyc/` | KycListPage + KycDetailModal | Yes | `/kyc` KYC review list (filters + email search, oldest-first); `/kyc/:id` detail modal — decrypted PII, presigned photos (TTL countdown + refresh), approve/reject (Developer view-only), audit trail. **CDD block (USDX-545)**: occupation / source of funds / annual income / transaction purpose / NPWP / PEP status + relation, enum values copied value-for-value from `partner_customer_kyc` so one legal entity does not run two CDD standards. `npwp` + `pepRelation` are PII → ADMIN only (`canReadCustomerPii`, `src/lib/pii.ts`); non-ADMIN sees `***` plus an "admin only" hint while a genuinely empty field shows an em dash, because "withheld" and "not collected" are different facts. The section renders even when the whole block is empty (customers VERIFIED before this ticket) with a note saying so — the alternative is a reviewer mistaking absent CDD for a complete record, which is the failure this ticket exists to close. |
| `kyb/` | KybListPage + KybDetailModal + KybFormPage + LegalEntityPicker | Yes | **`/kyb` KYB review (USDX-546)** — business-entity due diligence. A MANUAL flow (keputusan Mas Yan: KYB partner manual, bukan API), which is why this feature has a data-entry FORM as well as a review action: nothing but an operator creates these records. The LEGAL_ENTITY *account* already exists (`POST /api/v1/users` accepts it today); what was missing is somewhere to keep the entity's CDD data. `/kyb/new` = entity fields + UBO repeater (declared ownership may not exceed 100%; a record with zero UBOs has no due-diligence subject and is refused by the form AND flagged red in the list and the review screen). `/kyb/:id` = entity block + UBO cards (`identityNumber` is PII → ADMIN only) + **five FIXED document slots, each with its own upload** + approve / **reject with a MANDATORY reason**. The document slots mirror the backend exactly (PR #271, commits 5dc7254 + 307a292, migration 0077): one path column per document type — `akte` / `nib` / `npwp` / `skKemenkumham` / `ktpDireksi` — so `documents` is a MAP whose keys are always all present, `null` meaning the slot is empty to this viewer, and `urlExpiresAt` is a top-level sibling (not per slot). Consequences the UI honours rather than papers over: no `OTHER` (no column), no `fileName` / `sizeBytes` (no columns), so a reviewer identifies a document by its Indonesian SLOT LABEL — "Akta Pendirian", "NIB", "NPWP Badan", "SK Kemenkumham", "KTP Pengurus" — which is the more useful fact anyway. Labels live once in `KYB_DOCUMENT_SLOTS` (`src/lib/cdd.ts`), typed `Record<KybDocumentSlot, string>` so a new slot cannot be half-added. **Document upload — the three-step flow (`useUploadKybDocument`)**: `POST :id/documents/presign` `{ docKind, fileType, sizeBytes }` → `PUT` the bytes straight to storage with the ticket's `headers` VERBATIM and `credentials: 'omit'` (the desk session cookie must never reach the bucket host) → `POST :id/documents` `{ docKind, objectKey }`. This is what closed the gap that made the whole screen unable to finish a record: until it existed every slot was empty, approve could only answer `409 KYB_DOCUMENTS_INCOMPLETE`, and **no entity could reach VERIFIED from the back office** — reject worked, approve could not. `sizeBytes` is not advisory: the presigner signs it as `Content-Length`, the browser fills that header from the real file and forbids overriding it, so a guessed size makes storage refuse every PUT. The REQUEST vocabulary is lower snake_case (`kyb_akte` … `kyb_ktp_direksi`) while the RESPONSE slots stay camelCase; the one map between them is `KYB_DOCUMENT_SLOT_DOC_KINDS` (`src/lib/kybDocumentUpload.ts`), typed so a slot cannot be half-added. The limits enforced client-side are the server's own numbers — **5 MiB** (`MAX_FILE_SIZE_BYTES`) and **PDF / JPEG / PNG** (`KYB_ALLOWED_CONTENT_TYPES`, deliberately WITHOUT `image/heic`, which is valid for a KYC photo and not for a KYB document) — plus a **magic-byte sniff** mirroring the backend's `sniffContentType` at offset 0, because the extension and the Content-Type are both chosen by whoever picked the file and only the first bytes are not. Both server gates are mirrored in the UI rather than left to fail: the picker renders only for STAFF / MANAGER / ADMIN (DEVELOPER → 403) and only while the record is PENDING (anything else → `409 INVALID_STATUS`), so nobody is handed a control that can only fail. Every failure names its CAUSE (`describeKybUploadFailure`) on the ROW that refused — the operator is holding a customer's document and has to know whether the problem is the file or us. The attach response's `uploaded` map (all five columns, server-read) drives the slot state instead of a refetch: re-reading the detail decrypts entity PII and mints presigned URLs, so it writes a `pii_access_audit` row, and five uploads would manufacture five audited reads — one explicit "Reload record" button is offered instead. An empty slot therefore says so out loud rather than rendering blank — but the wording is role-aware, because the DEVELOPER role is never handed presigned URLs and reads all five as `null` whatever is on file; claiming "Not uploaded" there would be a false statement a reviewer could act on. **Approve is gated on documents server-side**: `409 KYB_DOCUMENTS_INCOMPLETE` carries `details.missing` naming every empty REQUIRED slot at once (`akte` · `nib` · `npwp` · `ktpDireksi` — `skKemenkumham` is conditional, a CV has none), and the screen highlights exactly those rows via `parseKybDocumentsIncomplete` (`src/lib/kybDocumentsError.ts`) instead of a generic toast, so the reviewer knows what to ask the customer for. That 409 is checked BEFORE the generic one — treating it as "someone else reviewed this" would refetch the record and hide the only actionable fact in the response. The FE keeps no copy of which slots are required: the rule is conditional (`REQUIRED_KYB_DOCUMENTS` in `kyb.service.ts` omits `skKemenkumham` because a CV has none) and lives with the backend, so the screen highlights whatever the server names and nothing here can drift out of step with it. The reason rule is enforced in THREE places — the dialog, `useRejectKyb`, and the API — because a dialog-only guard is bypassed by any other caller and the review trail then carries a rejection with no stated reason, which is exactly the record an auditor asks about. `LegalEntityPicker` is deliberately NOT `components/UserPicker.tsx`: that one filters `kycStatus=VERIFIED`, and an entity awaiting KYB is by definition not verified, so reusing it would have looked like reuse and behaved like a bug. **Endpoints are LIVE (USDX-546, 28 Aug 2026)** — the tables landed with backend PR #271 (migration `0077`) and the document routes with PR #275, so all seven `/api/v1/kyb*` paths are in `INTEGRATION_PATHS` and the KYB MSW handlers + data factories were DELETED, not left registered. Three response mismatches were found in the field-by-field reconciliation against `backend/src/modules/kyb/kyb.types.ts` and fixed HERE, because the backend is deployed and this side was built against a contract agreed in messages: (1) the LIST carries **no** `entityName`, `registrationNumber` or `uboCount` — the first two are encrypted columns a list query cannot read, so the queue shows `userName` (`users.name`, plaintext) plus `entityForm` and `submissionCount`; (2) every entity PII field on the DETAIL is `string | null` **and** arrives as the literal `'***'` for DEVELOPER (`maskFields` masks all six encrypted `kyb` columns), so `EntityValue` separates withheld from empty instead of printing three asterisks as if they were the name; (3) `kyb_entity_form` has TEN values, not the six the FE listed — `PT_PERORANGAN` / `PERKUMPULAN` / `BUMN` / `BUMD` were storable but unenterable. `validateKybRejectReason` also gained the backend's **10-character minimum** (`RejectKybDto @MinLength(10)` + two DB CHECKs), and `validateKybForm` now matches `CreateKybDto` on NIB (digits only), country (ISO alpha-2 uppercase), sector (120), addresses (255) and ownership (2 decimals) — every one of those was previously a 400 discovered after the whole form was typed. |
| `screening/` | ScreeningQueuePage + ScreeningDecisionModal + SanctionListsPage + SanctionListImportDialog | Yes | **`/screening` screening DTTOT & DPPSPM (USDX-588)** — POJK 8/2023 Pasal 53, satu-satunya temuan audit CDD yang wajib DAN bersanksi administratif. Dua sifat kontraknya membentuk seluruh fitur ini dan tidak boleh dilupakan: (1) `screening_results` **tidak memuat nama nasabah** — tabelnya append-only, dijaga dua trigger DB, jadi PII di sana jadi PII yang tidak bisa dihapus sweeper retensi; identitas subjek ditarik terpisah lewat `subjectType` + `subjectId`; (2) **entri daftar bukan PII** — DTTOT/DPPSPM adalah publikasi publik yang justru disebar agar dicocokkan, jadi `SanctionEntryPanel` merender apa adanya, sengaja TIDAK lewat `presentPii`. **Antrean** default `open=true`, yang berarti "masih menahan subjeknya", BUKAN "belum diputuskan": server menyaring `POTENTIAL_MATCH` + `decision.outcome IS DISTINCT FROM 'CLEARED'`, jadi temuan yang diputus `CONFIRMED_MATCH` tetap muncul — subjeknya memang masih tertahan — dan kolom Keputusan yang memisahkan "menunggu keputusan" dari "sudah diputus dan masih menahan". Urutan server `createdAt DESC` dan API tidak punya parameter urutan sama sekali; tiket meminta "skor tertinggi di atas", jadi skor ditonjolkan sebagai angka + **bilah yang diregangkan dari ambang 0.85** (bukan dari nol — seluruh antrean di atas ambang, jadi bilah dari nol membuat 0.86 dan 0.99 tampak sama panjang) alih-alih mengurutkan halaman yang dipaginasi server, yang akan tampak terurut sambil menaruh skor tertinggi halaman 2 di bawah skor terendah halaman 1. **Layar banding** menaruh data nasabah dan entri daftar berdampingan karena satu-satunya pertanyaannya adalah "apakah ini pihak yang sama"; sisi nasabah dibaca lewat `GET /api/v1/kyc/{id}` / `kyb/{id}` dengan **kunci cache yang SAMA** (`['kyc','detail',id]`) dengan layar review, sebab tiap pembacaan menulis satu baris `pii_access_audit` dan kunci terpisah akan membuat satu perbuatan jadi dua pembacaan teraudit; pembacaan itu juga hanya berjalan setelah temuannya dimuat, bukan saat antrean dirender. Panelnya sengaja SEMPIT (nama, tanggal/tempat lahir, negara, alamat) — layar ini bukan jalan memutar membaca seluruh PII nasabah, dan `identityNumber` / `npwp` tidak ditampilkan sama sekali karena entri DTTOT tidak memuat nomor identitas Indonesia sehingga tidak ada yang bisa dibandingkan dengannya. **`KYC_UBO` dinyatakan tidak bisa ditelusuri**, bukan dirender kosong: tidak ada endpoint yang mengambil satu baris `kyc_ubo` berdasarkan idnya (UBO hanya menempel pada `KybDetail`) dan temuan tidak membawa id KYB induknya — panel kosong akan terbaca sebagai "nasabah ini tidak punya data", arti yang berbeda dan bisa ditindaklanjuti keliru. **Dua keputusan** `CLEARED` / `CONFIRMED_MATCH` dengan alasan WAJIB min. 10 karakter, ditegakkan di TIGA tempat (dialog, `useDecideScreening`, API + CHECK `screening_results_row_shape`) karena gerbang yang hanya ada di dialog dilewati pemanggil lain dan jejak auditnya lalu membawa keputusan tanpa alasan — persis catatan yang ditanyakan pemeriksa; alasan inilah "hasil analisis" yang Pasal 63 ayat (2) huruf c wajibkan. Kotak alasan dikosongkan SAAT RENDER ketika temuan berganti (pola resmi React untuk "menyesuaikan state ketika prop berubah"), bukan di `useEffect` yang berjalan setelah paint — frame perantara itulah yang bisa mengirim alasan temuan sebelumnya ke berkas orang lain. **`/screening/lists`** (ADMIN + MANAGER, digerbangi di ROUTE karena menyembunyikan tombol saja meninggalkan halamannya sejauh satu URL) memuat impor tiga langkah: berkas dibaca dan **divalidasi seluruhnya di browser** (`previewSanctionCsv`, cermin `sanction-csv.ts` termasuk nomor barisnya) SEBELUM versi `DRAFT` dibuat, sebab tidak ada endpoint untuk menghapus versi DRAFT dan berkas cacat di baris 4.000 akan meninggalkan versi setengah terisi yang tidak bisa dibersihkan lewat API mana pun; jumlah entri diperlihatkan saat masih bisa membatalkan sesuatu; potongan dikirim BERURUTAN (`totalEntries` kumulatif — potongan paralel membuat angka terakhir belum tentu tertinggi dan bilah kemajuan bisa mundur) dan tiap potongan membawa baris header sendiri, karena server tidak mengingat keadaan antar panggilan dan potongan tanpa header akan menganggap entri pertamanya sebagai nama kolom — entri itu hilang diam-diam dari daftar sanksi. Mengaktifkan versi selalu **menawarkan pemindaian ulang**: Pasal 53 ayat (3) mewajibkan pemeriksaan sejak daftar DITERIMA, bukan hanya saat onboarding, dan server sengaja memisahkan aktivasi dari rescan — tanpa tawaran itu celah antara "daftar baru aktif" dan "nasabah lama sudah diperiksa dengannya" tidak pernah ditutup; `truncated: true` menawarkan lanjutan (rescan idempoten: menulis jejak baru, tidak mengubah apa pun). Endpoint-endpointnya **real-BE-only** — modulnya utuh di backend sejak USDX-585 dan kontraknya di `sot/api/screening.yaml` (USDX-600), jadi tidak pernah ada handler MSW yang ditulis untuknya; tesnya menyetel sendiri endpoint yang diujinya per skenario. |
| `mint/` | MintListPage + MintFormPage | Yes | `/mint` data table of mint requests; `/mint/new` mint OTC form. |
| `burn/` | BurnListPage + BurnFormPage + BurnRequestForm + BurnRequestInfoPanel | Yes | `/burn` data table of burn requests; `/burn/new` burn OTC form. |
| `transactions/` | TransactionsListPage + OrderDetailModal | Yes | `/transactions` read-only consumer order list — mint (USDX-206) + redeem (USDX-245); filter type/status (contextual RedeemStatus when type=REDEEM)/payment/safe + **Owner (partner/retail, USDX-547 — once partner orders exist the two populations share one table and an ops question is nearly always about one of them; the filter is server-side because the list is server-paginated and a client filter would hide rows on other pages and report the wrong total)**. **Partner column (USDX-547)**: its own column with `partners.display_name` + `partners.code`, **EMPTY for retail** (not "—", not "N/A" — both read as "failed to load"; empty reads as "does not apply"). It answers what the `(partner customer)` email marker does not: *which partner*, because a problematic partner order is chased with the PARTNER — the partner's customer has no relationship with USDX, their CDD lives at the partner and they receive no notification from us. Partner-ness is decided by `partner_id`, NOT by the email marker: a partner's own (`onBehalfOf: SELF`) order carries a real email and still counts. `external_reference` + `partner_customer_id` + on-behalf-of appear in the detail modal, with `external_reference` rendered in FULL (never middle-truncated — it is the number the partner quotes when reporting a problem, so ops must match it character for character); `/transactions/:id` detail modal — mint fee/spread/revenue + Safe links, or redeem spread jual / redeem+disbursement fee / net payout / bank (nama + nomor penuh + nama pemilik, un-mask USDX-270) / burn tx / payout ref (all roles). |
| `rate/` | RatePage + CurrentRateCard + RateUpdateForm + RateConfirmDialog | Yes | `/settings/rate` view + update base rate + spread beli/jual (admin-only update; USDX-207). |
| `fee/` | FeeConfigPage + CurrentFeeConfigCard + FeeConfigUpdateForm | Yes | `/settings/fee` view + update fee config — mint fee % + PG fee VA flat / QRIS % (USDX-207) + redeem fee % + disbursement fee flat (USDX-245); POST = full 5-field snapshot (422 VALIDATION_ERROR on invalid); admin-only update, read-only for non-admin. |
| `threshold/` | ThresholdPage + CurrentThresholdCard + ThresholdUpdateForm | Yes | `/settings/threshold` view + update Safe routing threshold (admin only). |
| `transparency/` | TransparencyPage + ReserveBalanceCard + LedgerEntryForm + LedgerConfirmDialog + LedgerHistoryTable + AttestationSection + AttestationUploadDialog + AttestationRevokeDialog | Yes | `/transparency` **append-only reserve ledger** (`catatan/KONTRAK-API-TRANSPARANSI.md`) + monthly attestation reports feeding the PUBLIC page on usdx.co.id. No draft, no publish button, no edit, no delete — an entry is public the moment it is recorded and corrections are new entries with a negative amount. The reserve figure is read from `data.balance` (whole ledger, server-computed), NEVER summed from the visible page. Recording goes through a confirm dialog that restates the amount, shows the resulting balance and states the append-only rule; the POST fires from the dialog only, and is **refused while the balance is unknown** — a correction without its running balance is the case that puts the published reserve underwater. **`idempotencyKey`** is minted once per form-filling attempt (`newIdempotencyKey`, NOT per click) and re-sent unchanged on retry: without it a 504 that hides an already-written row makes the operator press again and doubles the public reserve permanently, because append-only means the only fix is a negative entry. After any non-422 failure the ledger is re-read and the fresh balance shown before the retry button is re-enabled. Attestation upload is the **three-step flow** (`POST upload-url` with a REQUIRED `{ period, sizeBytes }` — the presigner signs `content-length` and the browser fills it from the real file, so a backend left guessing 403s every upload — → ticket returns `{ uploadUrl, fileKey, expiresAt, headers }` → `PUT` the bytes to storage sending the ticket's `headers` VERBATIM, with `credentials: 'omit'` so the session cookie never reaches the storage host → `POST` to register `{ period, title, fileKey }`), never multipart. The storage host must also be in the CSP **`connect-src`** (`img-src` governs `<img>`, not `fetch`). Files are capped at **5 MiB** — the same number the backend signs against — and sniffed for a real `%PDF-` header, because name and MIME type are both picker-controlled. The list is **server-paginated** (`page`/`take`): the backend defaults to 24 (the public page's own limit) and revoked rows are then filtered client-side, so without paging an old report could stay publicly downloadable with no way to revoke it here. `POST /ledger` answers **201** for a new entry and **200** for a replay of a known `idempotencyKey` **whose content is identical** (recording staff included); both are success, because surfacing a safe replay as an error is what makes an operator press again. The same key with **different** content is **409 `LEDGER_IDEMPOTENCY_KEY_CONFLICT`** and writes nothing — the 504-then-fix-the-typo case, where answering with the old entry leaves the wrong figure as the public reserve while the screen says "recorded". The back office treats it as an actionable failure: re-read the balance, show it, then offer to re-file under a NEW key (append-only, so the new entry sits BESIDE the old one, it does not replace it); the plain confirm button is locked meanwhile, since re-sending the same key can only earn the same 409. Amounts compare canonically — `"100667.4"` and `"100667.40"` are not a conflict. The key must be 16-200 characters (`LEDGER_IDEMPOTENCY_KEY_INVALID`) — the floor exists because a lazy key like `"retry"` collides across attempts and a collision DROPS a legitimate entry, a quieter failure than a duplicate. Neither key code maps to a form field: the operator typed nothing wrong. The MSW stubs are deliberately as strict as the backend (signed headers AND content-length, 28-digit `numeric(30,2)` ceiling, no `trim()` on `amount`, a JSON-number `amount` refused as `422 LEDGER_AMOUNT_INVALID` rather than crashing the stub, `fileKey` prefix, key-length 422, full CONTENT comparison behind the key so a changed amount is a 409 instead of a fake 200, bucket lookups behind `ATTESTATION_FILE_NOT_UPLOADED` / `ATTESTATION_FILE_TYPE_INVALID`, 404 on a double revoke) — a permissive mock is what let every earlier mismatch pass locally and fail in production. Note the split on 400s: the **attestation** DTOs use class-validator, so their shape errors are NestJS's own plain 400 (transparency is not in `V1_VALIDATION_422_ROUTES`), while `CreateLedgerEntryDto` is `@Allow()`-only by design — on the ledger route every condition, missing or mistyped fields included, comes back as its own named 422. Route is `RoleGuard`-ed to ADMIN + DEVELOPER (asserted against the REAL `appRoutes` in `AuthGuard.test.tsx`); every write is ADMIN (BE enforces 403). |
| `oncall/` | OncallContactsPage + OncallContactModal + OncallContactDeleteDialog | Yes | `/settings/oncall` kontak on-call insiden uang (USDX-485, audit P1-18) — CRUD penuh, **ADMIN-only termasuk untuk membaca** (nomor telepon = PII; daftarnya menentukan siapa yang dipanggil saat uang bermasalah). Banner memberi tahu kategori insiden mana yang belum punya penanggung jawab; dialog hapus memperingatkan saat kontak TERAKHIR sebuah kategori akan dibuang. |
| `profile/` | ProfilePage + PersonalDetailsForm + SecurityAccessSection + RecentActivityTimeline | Yes | Operator profile (reachable only via navbar dropdown). |

## Adding a Feature

1. Create folder under `src/features/`
2. Create page + hooks + (optional) modal/delete dialog/filter toolbar
3. Register route in `src/App.tsx`
4. Add sidebar nav item in `src/components/layout/Sidebar.tsx`
5. Add MSW handlers in `src/mocks/handlers.ts`
6. Add Vitest integration tests in `src/features/{feature}/__tests__/`
7. If the feature is critical to the operator flow, extend `e2e/smoke.spec.ts`
