# Empty = FE makes relative /api/* requests; Vite (dev) and Netlify (prod)
# proxy them server-side to the BE so the browser never sees a cross-origin
# request. Override with a fully-qualified URL only if you want to bypass
# the proxy and go direct (which requires BE CORS).
VITE_API_BASE_URL=
